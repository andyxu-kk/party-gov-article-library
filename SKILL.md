---
name: party-gov-article-library
description: 把人民日报、新华社、湖南日报等党政媒体优秀文章加工成轻量、可检索、可复用的知识卡片：提炼文章结构骨架与可迁移金句，整理为便于 AI 知识库检索的 Markdown 文本。
user-invocable: true
---

# 党政优秀文章知识库构建

## 一、任务定位

当用户提供人民日报、新华社、湖南日报及其他权威党政媒体的文章全文、截图转写文本或文章链接，并希望学习、分析、收藏、入库时，使用本 Skill。

本 Skill 的目标是把一篇已经由用户筛选出的优秀文章加工成**轻量、可检索、可复用的知识卡片**，而不是写二次研究报告。

知识卡片只回答两类问题：

1. 这篇文章的结构有什么值得借鉴；
2. 这篇文章有哪些金句可以直接调用。

除此之外，能不写就不写。知识库的目标不是内容越多越好，而是：检索快、信息密度高、重复少、可直接调用、长期积累后仍然清晰。

一次执行完成以下工作：

1. 判断文章最适合的知识库主类型；
2. 提炼文章结构骨架与结构价值；
3. 结合用户金句偏好样本，提炼值得长期积累的原文金句；
4. 生成适合 AI 检索的 Markdown 文件名与 Front Matter；
5. 输出最终可直接入库的 Markdown 文本。

不要把“分析”和“知识库整理”拆成两个独立任务，除非用户明确要求只做其中一步。

---

## 二、输入处理

### 1. 用户粘贴文章全文

直接分析全文。

### 2. 用户只提供文章链接

如果当前环境具备网页读取能力，先读取文章正文，再执行本 Skill。

优先使用原始发布媒体或权威转载来源，尽量核对：

- 正式主标题；
- 来源；
- 发布日期；
- 正文；
- 原有小标题（如有）。

如果网页无法读取正文，不得依据标题或搜索摘要猜测全文内容，应明确告知用户需要提供文章正文。

**抓取实战经验**：参考 `references/文章链接抓取经验.md`（人民日报老站正文在 `<div class="article">` 容器、页面为 UTF-8 编码等踩坑经验与 PowerShell 抓取模板）。

### 3. 用户同时指出“哪些句子是我认为的金句”

把这些句子视为用户对“金句审美”的高优先级校准样本：

- 用户明确指定的金句必须保留；
- 以这些样本反推用户偏好的表达特点；
- 在同一篇文章中优先继续寻找质量相当的表达；
- 不需要对用户指定的金句进行反驳或重新评判；
- 用户后续持续标记的金句，应优先于通用判断规则。

### 4. 金句偏好参考库

执行金句提炼前，如技能目录中存在 `references/金句偏好样本.md`，先读取其中的偏好规则。

该参考库用于校准“什么样的句子值得收藏”，而不是要求机械模仿其中的主题或句式。

特别注意：

- 参考其简洁性、观点性、节奏感、独立成句能力和迁移价值；
- 不把“励志感”本身视为金句标准；
- 对党政文章，应优先识别政策判断、工作方法、发展逻辑、改革理念、治理经验等具有材料复用价值的表达；
- 如果参考库与用户在当前任务中的明确判断冲突，以用户当前判断为最高优先级。

---

## 三、文章类型判断

文件名中的“文章类型”用于未来 AI 知识库的第一层快速检索，因此必须稳定、明确、可复用。

### 判断原则

不要按照报纸栏目分类，也不要使用“深阅读”“评论”“记者观察”等栏目名作为文章类型。

应按照“未来写材料时，我会因为什么主题检索这篇文章”来判断。

优先从以下主类型中选择一个：

- 党建引领
- 基层治理
- 乡村振兴
- 文旅发展
- 文化建设
- 产业发展
- 改革创新
- 城市发展
- 区域发展
- 生态文明
- 民生服务
- 营商环境
- 科技创新
- 人才工作
- 作风建设

如果文章确实明显不属于以上类型，可以新增一个简洁、稳定、通用的类型，但不要为了单篇文章创造过细分类。

### 多主题文章

如果一篇文章同时涉及多个主题：

- 文件名只保留一个“主类型”；
- 选择未来检索价值最高、最能代表文章核心写作价值的主题作为主类型；
- 其他主题放入 Front Matter 的“关键词”，不要在文件名堆叠多个类型。

---

## 四、文件命名规则

固定格式：

`文章类型_原文章名.md`

例如：

`乡村振兴_班彦村的10年奋进路.md`

### 命名要求

1. “文章类型”使用第三部分确定的主类型；
2. “原文章名”原则上使用文章正式主标题，不自行改写标题；
3. 报纸栏目前缀、版面说明、作者信息不写入文件名；
4. 如果原标题包含文件系统不允许的字符 `/ \ : * ? " < > |`，仅对这些字符做最小替换，不改变标题原意；
5. 不在文件名前添加日期、来源、序号，除非用户另有要求。

---

## 五、最终 Markdown 结构

每次执行最终只产出一份完整 Markdown 知识文件。

文件内容**严格只有**以下结构，不得增加第三个正文板块：

```md
---
标题: 文章正式标题
来源: 人民日报
日期: 2026-08-19
文章类型: 基层治理
关键词:
 - 基层工作
 - 调查研究
 - 实事求是
 - 长期主义
原文链接: https://……
---

# 一、文章结构

**结构骨架：**
心中有数 → 手中有策 → 行动有力

**结构说明：**
（一至两段简洁说明文章围绕什么展开、各部分是什么关系、这种结构为什么值得借鉴。）

# 二、金句

## 金句1

【原文】

基层是服务群众的最近一公里，也是政策落地的最后一公里。

【类型】

基层治理类

【可迁移应用】

基层治理、党建材料、基层定位。

## 金句2

【原文】

……

【类型】

……

【可迁移应用】

……
```

---

## 六、文章结构分析标准

### 结构骨架

**1 行**提炼文章的逻辑骨架，用 `A → B → C` 形式表达，不写多余说明。

**情况一：文章有正式小标题**

直接提炼原标题之间的逻辑。

例如：

```md
**结构骨架：**
以技兴业，指尖增收 → 顺时而为，田垄生金 → 顺势施策，山野寻宝
```

**情况二：文章没有正式小标题**

不要输出“原小标题：无小标题”，也不要为了满足模板而制造“小标题体系分析”。

应直接识别文章的隐性结构骨架。例如文章围绕“心中有数、手中有策、行动有力”展开，则直接输出：

```md
**结构骨架：**
心中有数 → 手中有策 → 行动有力
```

重点提炼文章真正的逻辑骨架，而不是判断它形式上有没有小标题。

### 结构说明

用 **1—2 段**简洁说明：

- 文章围绕什么展开；
- 各部分之间是什么关系（并列、递进、层层深入、总—分—总等）；
- 这种结构为什么值得借鉴（如标题统一句式、逻辑链条完整、适合套用到哪类材料）。

不做逐段新闻摘要；不复述大量人物、数字和案例细节；案例只在确实影响结构判断时简要提及。

目标是：AI 或人打开文件后，几秒钟就知道这篇文章的结构价值。

---

## 七、金句提炼标准

### 1. 只摘原文

金句的【原文】必须来自文章原句，不得自行润色、拼接、改写或仿写。

如果为了语义完整必须带上前后极少量文字，可以保留完整原句，但不要重组句子。

### 2. 先校准偏好，再宁缺毋滥

如存在 `references/金句偏好样本.md`，先用其中的偏好特征校准判断尺度；如用户在当前文章中明确标记了金句，则当前标记优先级最高。

不要为了凑数量，把普通事实句、数字句、记者过渡句都列为金句。

优先提炼真正具备以下一种或多种价值的句子：

- 能高度概括一个发展理念；
- 能把抽象政策说得简洁有力；
- 能总结工作方法；
- 能形成具有记忆点的判断；
- 能用于政府汇报、调研报告、规划策划等材料；
- 有鲜明画面感、比喻感或传播力；
- 短而有力，脱离原文局部语境后仍有较高借鉴价值；
- 具有明显的判断、转折、对照、递进、凝练概括或形象化表达特征。

不要因为一句话“像鸡汤”“像口号”或“足够短”就认定为金句。对于党政文章，材料复用价值高于单纯情绪感染力。

### 3. 金句数量

继续坚持宁缺毋滥。一般 3—8 条；特别优秀可适当增加；没有足够金句时可以少于 3 条。不要为了数量完整而降低标准。

### 4. 类型

【类型】使用最贴切的一项即可，优先使用：

- 发展理念类
- 工作方法类
- 政策表达类
- 改革创新类
- 产业发展类
- 文旅发展类
- 党建治理类
- 文化建设类
- 乡村振兴类
- 民生服务类
- 形象表达类

确有必要时可以使用其他简洁类型，但避免制造大量近义分类。

### 5. 可迁移应用

【可迁移应用】以**关键词式应用场景**为主，只罗列 2—5 个检索关键词，不写完整解释句。

例如：

- 原来：`适用于乡村振兴、基层治理汇报材料中论述正确政绩观与长远发展的段落。`
- 改为：`乡村振兴、基层治理、正确政绩观、长期主义。`

- 原来：`适用于领导讲话、调研报告中论述调查研究与政策落实相结合的段落。`
- 改为：`领导讲话、调研报告、调查研究、政策落实。`

原则：简洁、便于 AI 检索、不写完整句、一般 2—5 个应用关键词。

---

## 八、Front Matter 生成标准

最终 Front Matter 只保留以下字段：

```yaml
标题:
来源:
日期:
文章类型:
关键词:
原文链接:
```

### 标题

使用文章正式原标题。

### 来源

例如：人民日报 / 新华社 / 湖南日报。

### 日期

统一格式：`YYYY-MM-DD`。

### 文章类型

只保留一个主类型。

### 关键词

建议 4—6 个。关键词用于 AI 检索，应优先体现：

- 核心议题；
- 工作方法；
- 重要理念；
- 典型应用主题。

避免堆叠地点、人名、过细概念。

### 原文链接

如果用户提供文章链接，则保留。如果没有原文链接，则删除这一行，不要填写“无”。

**已删除字段（不要再生成）：** 核心主题、适用场景、结构特征。这些信息已能从正文或金句中体现，没有必要重复。

---

## 九、知识库目录解析

本 Skill 不硬编码任何机器专属路径，知识库目录按以下优先级解析（从上到下，命中即用）：

1. **用户当场指定**：本次对话中用户明确说了保存位置，以此为准；
2. **本机配置文件**：读取 skill 目录下的 `config.json`（参见 `config.example.json` 模板），取其中的 `library_path` 字段；
3. **默认值**：`E:\andy的大脑\策划师修炼\党政文章库`（仅作为兜底，各电脑实际路径以配置为准）。

新电脑安装本 Skill 后，如库路径与默认值不同，在 skill 目录下创建 `config.json`：

```json
{
  "library_path": "D:\\我的文档\\党政文章库"
}
```

注意：`config.json` 属于本机个性化配置，**不提交到 GitHub**（已在 `.gitignore` 中排除）。执行时若配置文件存在但路径不存在，应提示用户核对配置，不得静默使用默认值。

---

## 十、输出规则

### 默认输出

先单独给出：

`文件名：乡村振兴_班彦村的10年奋进路.md`

然后输出完整 Markdown 正文。

不要额外写长篇说明、评价、使用教程或第二份分析稿。

### 如果环境允许直接写文件

如果知识库目录已按第九节解析成功，并且当前环境具备文件写入权限：

1. 按本 Skill 的命名规则生成 `.md` 文件；
2. 保存到解析出的知识库目录；
3. **按文章主类型分子文件夹保存**：在知识库目录下先按主类型创建子文件夹（如 `基层治理\`、`党建引领\`、`乡村振兴\`），再把文件保存到对应类型子文件夹中，方便日后按类检索（2026-08-21 主子确认）；
4. 不重复询问已知路径；
5. 保存完成后只简洁告知文件名和保存结果。

如果无法解析出有效目录，不得自行猜测目录；输出文件名和完整 Markdown 即可。

---

## 十一、严格禁止

- 不做普通新闻摘要；
- 不写“三、写作方法总结”之类额外板块；
- 不输出“原小标题：无小标题”之类的模板填充；
- 不逐个小标题分别分析；
- 不给每条金句增加“价值分析”“修辞分析”“为什么好”“使用建议”“仿写句”等字段；
- 不把 AI 自己创作的句子冒充原文金句；
- 不为了显得完整而强行提炼低质量金句；
- 不把来源、栏目、作者当作文章类型；
- 不随意改写原文章标题；
- 不生成“核心主题、适用场景、结构特征”等已删除字段；
- 不在最终知识文件中加入与长期检索无关的寒暄或说明。

---

## 十二、质量自检

输出前快速检查：

1. 文件名是否为“主类型_原标题.md”？
2. 主类型是否真正有助于未来检索？
3. Front Matter 是否只含「标题、来源、日期、文章类型、关键词、原文链接」六项，且无链接时已删除该行？
4. 关键词是否为 4—6 个、避免地点人名堆叠？
5. 正文是否严格只有「一、文章结构」和「二、金句」两个板块？
6. 结构骨架是否为 1 行、以 `A → B → C` 表达？
7. 结构说明是否为 1—2 段，没有逐段摘要和大量案例复述？
8. 无小标题文章是否直接识别了隐性结构骨架，而非输出“无小标题”？
9. 金句是否全部为原文原句？
10. 每条金句是否只有【原文】【类型】【可迁移应用】三个字段，且【可迁移应用】为 2—5 个关键词？
11. 是否删除了低质量、仅陈述事实的“伪金句”？
12. 如存在金句偏好参考库，金句筛选是否参考了偏好特征，但没有机械模仿励志语气？
13. 如用户本次明确标记了金句，是否以用户当前判断作为最高优先级？

全部满足后再输出。
