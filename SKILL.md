---
name: party-gov-article-library
description: 分析人民日报、新华社、湖南日报等党政媒体优秀文章的整体结构与小标题体系，参考用户金句偏好提炼可迁移表达，并整理为便于 AI 知识库检索的 Markdown 文本。
user-invocable: true
---

# 党政优秀文章知识库构建

## 一、任务定位

当用户提供人民日报、新华社、湖南日报及其他权威党政媒体的文章全文、截图转写文本或文章链接，并希望学习、分析、收藏、入库时，使用本 Skill。

本 Skill 的目标不是做新闻摘要，也不是泛泛评价文章好坏，而是把一篇已经由用户筛选出的优秀文章加工成可长期复用的“党政写作知识资产”。

一次执行完成以下工作：

1. 判断文章最适合的知识库主类型；
2. 提炼文章整体结构和小标题体系规律；
3. 结合用户金句偏好样本，提炼值得长期积累的原文金句；
4. 生成适合 AI 检索的 Markdown 文件名与 Front Matter；
5. 输出最终可直接入库的 Markdown 文本。

不要把“分析”和“知识库整理”拆成两个独立任务，除非用户明确要求只做其中一步。

---

## 二、输入处理

### 1. 用户粘贴文章全文

直接分析全文。

### 2. 用户只提供文章链接

如果当前环境具备网页读取能力，先读取文章正文，再执行本 Skill。

优先使用原始发布媒体或权威转载来源，尽量核对：

- 正式主标题；
- 来源；
- 发布日期；
- 正文；
- 原有小标题。

如果网页无法读取正文，不得依据标题或搜索摘要猜测全文内容，应明确告知用户需要提供文章正文。

**抓取实战经验**：参考 `references/文章链接抓取经验.md`（人民日报老站正文在 `<div class="article">` 容器、页面为 UTF-8 编码等踩坑经验与 PowerShell 抓取模板）。

### 3. 用户同时指出“哪些句子是我认为的金句”

把这些句子视为用户对“金句审美”的高优先级校准样本：

- 用户明确指定的金句必须保留；
- 以这些样本反推用户偏好的表达特点；
- 在同一篇文章中优先继续寻找质量相当的表达；
- 不需要对用户指定的金句进行反驳或重新评判；
- 用户后续持续标记的金句，应优先于通用判断规则。

### 4. 金句偏好参考库

执行金句提炼前，如技能目录中存在 `references/金句偏好样本.md`，先读取其中的偏好规则。

该参考库用于校准“什么样的句子值得收藏”，而不是要求机械模仿其中的主题或句式。

特别注意：

- 参考其简洁性、观点性、节奏感、独立成句能力和迁移价值；
- 不把“励志感”本身视为金句标准；
- 对党政文章，应优先识别政策判断、工作方法、发展逻辑、改革理念、治理经验等具有材料复用价值的表达；
- 如果参考库与用户在当前任务中的明确判断冲突，以用户当前判断为最高优先级。

---

## 三、文章类型判断

文件名中的“文章类型”用于未来 AI 知识库的第一层快速检索，因此必须稳定、明确、可复用。

### 判断原则

不要按照报纸栏目分类，也不要使用“深阅读”“评论”“记者观察”等栏目名作为文章类型。

应按照“未来写材料时，我会因为什么主题检索这篇文章”来判断。

优先从以下主类型中选择一个：

- 党建引领
- 基层治理
- 乡村振兴
- 文旅发展
- 文化建设
- 产业发展
- 改革创新
- 城市发展
- 区域发展
- 生态文明
- 民生服务
- 营商环境
- 科技创新
- 人才工作
- 作风建设

如果文章确实明显不属于以上类型，可以新增一个简洁、稳定、通用的类型，但不要为了单篇文章创造过细分类。

### 多主题文章

如果一篇文章同时涉及多个主题：

- 文件名只保留一个“主类型”；
- 选择未来检索价值最高、最能代表文章核心写作价值的主题作为主类型；
- 其他主题放入 Front Matter 的“关键词”和“适用场景”，不要在文件名堆叠多个类型。

---

## 四、文件命名规则

固定格式：

`文章类型_原文章名.md`

例如：

`乡村振兴_班彦村的10年奋进路.md`

### 命名要求

1. “文章类型”使用第三部分确定的主类型；
2. “原文章名”原则上使用文章正式主标题，不自行改写标题；
3. 报纸栏目前缀、版面说明、作者信息不写入文件名；
4. 如果原标题包含文件系统不允许的字符 `/ \\ : * ? \" < > |`，仅对这些字符做最小替换，不改变标题原意；
5. 不在文件名前添加日期、来源、序号，除非用户另有要求。

---

## 五、最终 Markdown 结构

每次执行最终只产出一份完整 Markdown 知识文件。

文件内容必须遵循以下结构：

```md
---
标题: 原文章正式标题
来源: 人民日报/新华社/湖南日报/其他来源
日期: YYYY-MM-DD
文章类型: 主类型
核心主题: 用一句简洁的话概括文章真正讨论的问题
关键词:
  - 关键词1
  - 关键词2
  - 关键词3
  - 关键词4
  - 关键词5
适用场景:
  - 场景1
  - 场景2
  - 场景3
结构特征:
  - 特征1
  - 特征2
---

# 一、文章结构

## 全文结构

（整体说明文章围绕什么主题、按照什么逻辑展开、各部分如何共同支撑主题。控制在一至三段，不做逐段摘要。）

## 小标题体系

**原小标题：** 小标题1 → 小标题2 → 小标题3

（用一段话整体分析小标题之间的逻辑关系、命名规律、语言特征及可借鉴之处。不要逐个小标题分别分析。）

# 二、金句提炼

## 金句1

【原文】

……

【类型】

……

【可迁移应用】

……

## 金句2

【原文】

……

【类型】

……

【可迁移应用】

……
```

---

## 六、文章结构分析标准

### 1. 全文结构

重点回答三个问题：

- 文章围绕什么核心主题展开？
- 文章采用什么逻辑组织材料？
- 各层内容之间是什么关系？

可以识别但不限于以下结构：

- 时间演进；
- 问题—破题—成效；
- 现象—原因—路径；
- 总—分—总；
- 并列展开；
- 递进展开；
- 场景切换；
- 人物/案例串联；
- 产业链展开；
- 从实践到理念；
- 从局部到整体；
- 从基础建设到产业升级；
- 从单点突破到系统推进。

不要机械套用术语。应结合文章实际，用自然、准确的语言说明。

### 2. 小标题体系

只做“整体分析”，不要逐个解释。

必须先完整列出文章原有小标题，再分析它们作为一组为什么成立。

重点观察：

- 并列、递进还是层层深入；
- 是否形成统一句式；
- 前后半句是否分别承担“路径—结果”“动作—场景”“理念—成效”等功能；
- 是否使用四字短语、对仗、动词结构、意象化表达；
- 是否既有政策表达感，又有传播性和画面感；
- 这种标题组合未来适合在哪类材料中借鉴。

分析的目的不是解释字面意思，而是提炼“这一组标题是怎样被设计出来的”。

---

## 七、金句提炼标准

### 1. 只摘原文

金句的【原文】必须来自文章原句，不得自行润色、拼接、改写或仿写。

如果为了语义完整必须带上前后极少量文字，可以保留完整原句，但不要重组句子。

### 2. 先校准偏好，再宁缺毋滥

如存在 `references/金句偏好样本.md`，先用其中的偏好特征校准判断尺度；如用户在当前文章中明确标记了金句，则当前标记优先级最高。

不要为了凑数量，把普通事实句、数字句、记者过渡句都列为金句。

优先提炼真正具备以下一种或多种价值的句子：

- 能高度概括一个发展理念；
- 能把抽象政策说得简洁有力；
- 能总结工作方法；
- 能形成具有记忆点的判断；
- 能用于政府汇报、调研报告、规划策划等材料；
- 有鲜明画面感、比喻感或传播力；
- 短而有力，脱离原文局部语境后仍有较高借鉴价值；
- 具有明显的判断、转折、对照、递进、凝练概括或形象化表达特征。

不要因为一句话“像鸡汤”“像口号”或“足够短”就认定为金句。对于党政文章，材料复用价值高于单纯情绪感染力。

一般提炼 3—8 条；文章确实没有那么多时可以更少，优秀表达很多时可以适当增加，不设硬性数量。

### 3. 类型

【类型】使用最贴切的一项即可，优先使用：

- 发展理念类
- 工作方法类
- 政策表达类
- 改革创新类
- 产业发展类
- 文旅发展类
- 党建治理类
- 文化建设类
- 乡村振兴类
- 民生服务类
- 形象表达类

确有必要时可以使用其他简洁类型，但避免制造大量近义分类。

### 4. 可迁移应用

【可迁移应用】只回答“这句话以后能在哪里用”。

要求具体、简洁，例如：

`适用于乡村振兴调研报告、产业发展汇报中阐述“因地制宜、找准路径”的段落。`

不要在这里再次分析句子的修辞、价值或优点。

---

## 八、Front Matter 生成标准

### 核心主题

只写一句话，概括文章最核心的议题，不写新闻摘要。

### 关键词

建议 5—8 个。

关键词要兼顾：

- 文章主题；
- 工作方法；
- 典型场景；
- 未来检索词。

避免只有地点、人名等低复用词。

### 适用场景

建议 2—5 个。

从未来写作调用角度填写，例如：

- 政府工作汇报
- 调研报告
- 乡村振兴规划
- 文旅发展策划
- 党建品牌方案
- 领导讲话材料
- 经验交流材料

### 结构特征

建议 1—3 项，使用短语描述文章最值得检索的结构特征，例如：

- 三段式并列结构
- 产业路径递进
- 小切口呈现大发展
- 案例串联
- 问题—破题—成效

这部分用于未来 AI 搜索“有没有类似结构文章”时提高召回率。

---

## 九、知识库目录解析

本 Skill 不硬编码任何机器专属路径，知识库目录按以下优先级解析（从上到下，命中即用）：

1. **用户当场指定**：本次对话中用户明确说了保存位置，以此为准；
2. **本机配置文件**：读取 skill 目录下的 `config.json`（参见 `config.example.json` 模板），取其中的 `library_path` 字段；
3. **默认值**：`E:\andy的大脑\策划师修炼\党政文章库`（仅作为兜底，各电脑实际路径以配置为准）。

新电脑安装本 Skill 后，如库路径与默认值不同，在 skill 目录下创建 `config.json`：

```json
{
  "library_path": "D:\\我的文档\\党政文章库"
}
```

注意：`config.json` 属于本机个性化配置，**不提交到 GitHub**（已在 `.gitignore` 中排除）。执行时若配置文件存在但路径不存在，应提示用户核对配置，不得静默使用默认值。

---

## 十、输出规则

### 默认输出

先单独给出：

`文件名：乡村振兴_班彦村的10年奋进路.md`

然后输出完整 Markdown 正文。

不要额外写长篇说明、评价、使用教程或第二份分析稿。

### 如果环境允许直接写文件

如果知识库目录已按第九节解析成功，并且当前环境具备文件写入权限：

1. 按本 Skill 的命名规则生成 `.md` 文件；
2. 保存到解析出的知识库目录；
3. **按文章主类型分子文件夹保存**：在知识库目录下先按主类型创建子文件夹（如 `基层治理\`、`党建引领\`、`乡村振兴\`），再把文件保存到对应类型子文件夹中，方便日后按类检索（2026-08-21 主子确认）；
4. 不重复询问已知路径；
5. 保存完成后只简洁告知文件名和保存结果。

如果无法解析出有效目录，不得自行猜测目录；输出文件名和完整 Markdown 即可。

---

## 十一、严格禁止

- 不做普通新闻摘要；
- 不写“四、文章整体写作方法总结”之类额外板块；
- 不逐个小标题分别分析；
- 不给每条金句增加“价值分析”“为什么好”等字段；
- 不把 AI 自己创作的句子冒充原文金句；
- 不为了显得完整而强行提炼低质量金句；
- 不把来源、栏目、作者当作文章类型；
- 不随意改写原文章标题；
- 不在最终知识文件中加入与长期检索无关的寒暄或说明。

---

## 十二、质量自检

输出前快速检查：

1. 文件名是否为“主类型_原标题.md”？
2. 主类型是否真正有助于未来检索？
3. 全文结构是否是整体分析，而不是新闻摘要？
4. 是否完整列出了原小标题，并只做了一段整体分析？
5. 金句是否全部为原文原句？
6. 每条金句是否只有【原文】【类型】【可迁移应用】三个字段？
7. 是否删除了低质量、仅陈述事实的“伪金句”？
8. Front Matter 是否包含未来检索需要的关键词、适用场景和结构特征？
9. 最终是否只保留“文章结构 + 金句提炼”两个正文板块？
10. 如存在金句偏好参考库，金句筛选是否参考了偏好特征，但没有机械模仿励志语气？
11. 如用户本次明确标记了金句，是否以用户当前判断作为最高优先级？

全部满足后再输出。
